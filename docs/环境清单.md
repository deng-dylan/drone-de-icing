# XTDrone 多机仿真项目 — 环境清单

> 记录时间：2026-03-30  
> 用途：多机项目基线环境，打包给队友时附带此文档

---

## 系统层

| 项目 | 值 |
|------|-----|
| Windows 版本 | 10.0.26200.8037 |
| WSL 版本 | 2.6.3.0 |
| WSL 内核 | 6.6.87.2-1 |
| WSLg 版本 | 1.0.71 |
| Ubuntu 版本 | 20.04 (Focal) |

---

## 仿真层

| 项目 | 值 |
|------|-----|
| Gazebo 版本 | 11（需确认 `gazebo --version` 输出） |
| PX4 版本 | v1.13.2（分支 `xtdrone/dev`） |
| PX4 路径 | `~/PX4_Firmware` |
| XTDrone 路径 | `~/XTDrone` |
| Gazebo 模型目录 | `~/.gazebo/models/`（已有大量模型） |

---

## ROS 层

| 项目 | 值 |
|------|-----|
| ROS 版本 | Noetic |
| catkin 工作空间 | `~/catkin_ws` |
| catkin_ws/src 内容 | `gazebo_ros_pkgs` |
| MAVROS | 已安装（apt，路径 `/opt/ros/noetic/share/mavros`） |
| mavros_extras | 已安装（apt，路径 `/opt/ros/noetic/share/mavros_extras`） |
| catkin_ws setup | 正常 |

---

## 地面站层

| 项目 | 值 |
|------|-----|
| QGroundControl | **Windows 端运行**（WSL 内未找到） |

---

## .bashrc 环境配置

```bash
# 第 118-119 行
source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash
```

**状态：干净。** 仅 source 了 ROS1，无 ROS2 冲突。

---

## 每次开终端标准 source 命令

```bash
# 大部分已在 .bashrc 自动执行，如需手动加载 PX4/XTDrone：
source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash
source ~/PX4_Firmware/Tools/setup_gazebo.bash ~/PX4_Firmware ~/PX4_Firmware/build/px4_sitl_default
export ROS_PACKAGE_PATH=$ROS_PACKAGE_PATH:~/PX4_Firmware:~/PX4_Firmware/Tools/sitl_gazebo
```

---

## 待确认项

1. **QGroundControl 连接方式**：Windows 端 QGC 是否能连接 WSL2 内的 PX4 SITL？（涉及网络转发）
2. **Gazebo 版本**：请跑 `gazebo --version` 确认是否为 11.x
3. **XTDrone 适配状态**：XTDrone 的 `sitl_config` 和 `control` 目录内容是否完整

---

## 目录速查

| 用途 | 路径 |
|------|------|
| PX4 固件 | `~/PX4_Firmware` |
| XTDrone 源码 | `~/XTDrone` |
| catkin 工作空间 | `~/catkin_ws` |
| Gazebo 模型 | `~/.gazebo/models` |
| ROS Noetic | `/opt/ros/noetic` |
| MAVROS | `/opt/ros/noetic/share/mavros` |
